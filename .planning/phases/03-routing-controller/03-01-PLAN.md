---
phase: 03-routing-controller
plan: 01
type: execute
wave: 1
depends_on:
  - 02-03
files_modified:
  - src/main/java/com/ae2channelrouter/block/RoutingControllerBlock.java
  - src/main/java/com/ae2channelrouter/tile/RoutingControllerTile.java
  - src/main/java/com/ae2channelrouter/block/ModBlocks.java
autonomous: true
requirements:
  - CTRL-01
must_haves:
  truths:
    - RoutingControllerBlock is a full block (not cable-like)
    - RoutingControllerTile extends AEBaseRouterTile
    - Controller implements IRoutingDevice with DeviceType.CONTROLLER
    - Block and tile are registered in ModBlocks
  artifacts:
    - path: src/main/java/com/ae2channelrouter/block/RoutingControllerBlock.java
      provides: "Routing controller block with full block rendering"
      min_lines: 60
    - path: src/main/java/com/ae2channelrouter/tile/RoutingControllerTile.java
      provides: "Controller tile with channel pool data structures"
      min_lines: 100
    - path: src/main/java/com/ae2channelrouter/block/ModBlocks.java
      provides: "Block and tile registration"
      contains: "routingController"
  key_links:
    - from: RoutingControllerTile
      to: AEBaseRouterTile
      via: "extends AEBaseRouterTile"
    - from: RoutingControllerBlock
      to: RoutingControllerTile
      via: "setTileEntity()"
    - from: ModBlocks
      to: RoutingControllerBlock
      via: "registerBlocks()"
---

<objective>
Create the routing controller block and tile foundation. This is the central hub for channel allocation.

Purpose: Establish the controller's physical presence and basic AE2 integration.
Output: Controller block can be placed in world, tile entity tracks channel pool data structures.
</objective>

<execution_context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-routing-controller/03-CONTEXT.md
@.planning/phases/03-routing-controller/03-RESEARCH.md
</execution_context>

<context>
- Controller is a FULL BLOCK (not thin like cables)
- Extends AEBaseRouterTile for AE2 integration
- Implements IRoutingDevice with DeviceType.CONTROLLER
- Needs data structures for: detected controllers, allocated channels, connected terminals
- Block should have distinct appearance (crafting table-ish or special texture)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RoutingControllerTile</name>
  <files>src/main/java/com/ae2channelrouter/tile/RoutingControllerTile.java</files>
  <read_first>
    - src/main/java/com/ae2channelrouter/tile/AEBaseRouterTile.java
    - src/main/java/com/ae2channelrouter/api/IRoutingDevice.java
  </read_first>
  <action>
    Create RoutingControllerTile extending AEBaseRouterTile.

    Location: src/main/java/com/ae2channelrouter/tile/RoutingControllerTile.java

    Implementation requirements:
    - Extend AEBaseRouterTile
    - Implement IRoutingDevice with DeviceType.CONTROLLER
    - Add fields for channel pool tracking:
      - Set<TileController> detectedControllers (use HashSet or similar)
      - Map<String, Integer> terminalAllocations (terminal_id -> channels allocated)
      - int routingChannelId (default 0, configurable)
    - Implement abstract methods from AEBaseRouterTile
    - Add getter methods for channel stats
    - Override gridChanged() to trigger controller detection (placeholder for Wave 2)

    Key methods to implement:
    - onGridConnectionStateChanged(boolean connected)
    - getItemStackFromTile()
    - getInternalInventory() (return null)
    - onChangeInventory() (empty)
    - getAccessibleSlotsBySide() (return empty array)
    - getLocation() (return new DimensionalCoord(this))
    - updateEntity() or onTick() for periodic updates

    Data structure initialization:
    ```java
    private Set<TileController> detectedControllers = new HashSet<>();
    private Map<UUID, Integer> terminalAllocations = new HashMap<>();
    private int routingChannelId = 0;
    private int totalChannels = 0;
    private int allocatedChannels = 0;
    ```
  </action>
  <acceptance_criteria>
    - File exists and extends AEBaseRouterTile
    - Implements IRoutingDevice with DeviceType.CONTROLLER
    - Has data structures for controller tracking and allocations
    - Overrides all required abstract methods
    - Compiles without errors
  </acceptance_criteria>
  <verify>
    <automated>grep -n "extends AEBaseRouterTile" src/main/java/com/ae2channelrouter/tile/RoutingControllerTile.java && grep -n "DeviceType.CONTROLLER" src/main/java/com/ae2channelrouter/tile/RoutingControllerTile.java</automated>
  </verify>
  <done>RoutingControllerTile created with channel pool data structures</done>
</task>

<task type="auto">
  <name>Task 2: Create RoutingControllerBlock</name>
  <files>src/main/java/com/ae2channelrouter/block/RoutingControllerBlock.java</files>
  <read_first>
    - src/main/java/com/ae2channelrouter/block/RoutingCableBlock.java (for patterns)
    - D:/Desktop/LYC/code/Sources/Applied-Energistics-2-Unofficial-rv3-beta-695-GTNH/src/main/java/appeng/block/networking/BlockController.java (for reference)
  </read_first>
  <action>
    Create RoutingControllerBlock extending AEBaseBlock.

    Location: src/main/java/com/ae2channelrouter/block/RoutingControllerBlock.java

    Implementation:
    - Extend AEBaseBlock
    - Full block bounds (0 to 1, not thin like cables)
    - Register tile entity using setTileEntity(RoutingControllerTile.class)
    - Material: iron (appropriate for a controller device)
    - Harvest level: pickaxe
    - Creative tab: AE2 or custom tab

    Block properties:
    - isOpaqueCube() = true (full block)
    - renderAsNormalBlock() = true
    - getCollisionBoundingBox() = full block bounds

    Right-click behavior (for Wave 3):
    - onBlockActivated() - open GUI (placeholder for now, just return true)
  </action>
  <acceptance_criteria>
    - File exists and extends AEBaseBlock
    - Is a full block (not cable-like)
    - Registers RoutingControllerTile
    - Has proper material and properties
    - Compiles without errors
  </acceptance_criteria>
  <verify>
    <automated>grep -n "extends AEBaseBlock" src/main/java/com/ae2channelrouter/block/RoutingControllerBlock.java && grep -n "RoutingControllerTile" src/main/java/com/ae2channelrouter/block/RoutingControllerBlock.java</automated>
  </verify>
  <done>RoutingControllerBlock created as full block</done>
</task>

<task type="auto">
  <name>Task 3: Register in ModBlocks</name>
  <files>src/main/java/com/ae2channelrouter/block/ModBlocks.java</files>
  <read_first>
    - src/main/java/com/ae2channelrouter/block/ModBlocks.java
  </read_first>
  <action>
    Update ModBlocks.java to register RoutingControllerBlock and RoutingControllerTile.

    Changes needed:
    1. Add routingController field:
       ```java
       public static AEBaseBlock routingController;
       ```

    2. In registerBlocks():
       ```java
       routingController = new RoutingControllerBlock();
       GameRegistry.registerBlock(routingController, BLOCK_ROUTING_CONTROLLER);
       AE2ChannelRouter.INSTANCE.getLogger().info("Registered routing controller block");
       ```

    3. In registerTileEntities():
       ```java
       GameRegistry.registerTileEntity(RoutingControllerTile.class, AE2ChannelRouter.MOD_ID + ":" + TILE_ROUTING_CONTROLLER);
       AE2ChannelRouter.INSTANCE.getLogger().info("Registered routing controller tile entity");
       ```
       (Replace the existing AEBaseRouterTile registration with this)

    4. Add import for RoutingControllerTile
  </action>
  <acceptance_criteria>
    - routingController field exists
    - Block registered in registerBlocks()
    - Tile entity registered in registerTileEntities()
    - AEBaseRouterTile placeholder registration replaced
  </acceptance_criteria>
  <verify>
    <automated>grep -n "routingController" src/main/java/com/ae2channelrouter/block/ModBlocks.java | head -5</automated>
  </verify>
  <done>Routing controller registered in ModBlocks</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `./gradlew build` to verify compilation
2. Check that block can be placed in world (creative inventory)
3. Verify tile entity is created and connects to AE2 network
</verification>

<success_criteria>
- RoutingControllerTile extends AEBaseRouterTile and implements IRoutingDevice
- RoutingControllerBlock is a full block with proper registration
- Block and tile are registered in ModBlocks
- Data structures exist for controller detection and channel allocation
- Build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-routing-controller/03-01-SUMMARY.md`
</output>
