---
phase: 02-routing-cable
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/main/java/com/ae2channelrouter/block/RoutingCableBlock.java
  - src/main/java/com/ae2channelrouter/client/render/RoutingCableRender.java
  - src/main/java/com/ae2channelrouter/AE2ChannelRouter.java
autonomous: true
requirements:
  - CABL-03
must_haves:
  truths:
    - Cable renders with orange glass texture
    - Cable is visually distinct from AE2 gray/black cables
    - Cable renders connections to adjacent routing devices
  artifacts:
    - path: src/main/java/com/ae2channelrouter/client/render/RoutingCableRender.java
      provides: "Custom rendering for orange glass cable"
      min_lines: 100
    - path: src/main/java/com/ae2channelrouter/block/RoutingCableBlock.java
      provides: "Block with custom renderer registered"
      contains: "render"
  key_links:
    - from: RoutingCableBlock
      to: RoutingCableRender
      via: "Custom renderer registration"
    - from: AE2ChannelRouter
      to: RoutingCableRender
      via: "Client proxy registration"
---

<objective>
Implement custom rendering for routing cables with orange glass style, visually distinct from AE2 cables.

Purpose: Provide visual distinction between routing cables and standard AE2 cables.
Output: Orange glass cable renders in-world with proper connections.
</objective>

<execution_context>
@C:/Users/hjlyc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hjlyc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-routing-cable/02-CONTEXT.md
@.planning/phases/02-routing-cable/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RoutingCableRender</name>
  <files>src/main/java/com/ae2channelrouter/client/render/RoutingCableRender.java</files>
  <read_first>
    - D:/CODE/Sources/Applied-Energistics-2-Unofficial-rv3-beta-695-GTNH/src/main/java/appeng/parts/networking/PartCable.java (rendering patterns)
    - src/main/java/com/ae2channelrouter/tile/RoutingCableTile.java
  </read_first>
  <action>
    Create custom renderer for routing cables.

    Location: src/main/java/com/ae2channelrouter/client/render/RoutingCableRender.java

    Implementation:
    - Create class implementing ISimpleBlockRenderingHandler or extend AE2's rendering patterns
    - Use vanilla RenderBlocks for rendering (1.7.10 compatibility)
    - Orange color: RGB (255, 165, 0) - similar to AE2 orange but distinct
    - Glass-like appearance: slight transparency, reflective

    Rendering approach:
    1. Render central cable block (6/16 to 10/16 bounds)
    2. Render connections to adjacent routing devices
    3. Use simple colored rendering without complex textures

    For 1.7.10 compatibility, use:
    - RenderBlocks.setRenderBounds() for cable geometry
    - RenderBlocks.renderBlockByRenderType() for standard rendering
    - Custom color overlay for orange tint

    Key methods:
    - renderWorldBlock() - main rendering
    - shouldRender3DInInventory() - return true
    - getRenderId() - return unique render ID
  </action>
  <acceptance_criteria>
    - File RoutingCableRender.java exists in client/render package
    - Class implements ISimpleBlockRenderingHandler
    - Implements renderWorldBlock() method
    - Uses orange color for cable rendering
  </acceptance_criteria>
  <verify>
    <automated>grep -n "class RoutingCableRender" src/main/java/com/ae2channelrouter/client/render/RoutingCableRender.java && grep -n "ISimpleBlockRenderingHandler" src/main/java/com/ae2channelrouter/client/render/RoutingCableRender.java</automated>
  </verify>
  <done>RoutingCableRender created with orange glass style</done>
</task>

<task type="auto">
  <name>Task 2: Register custom renderer</name>
  <files>src/main/java/com/ae2channelrouter/AE2ChannelRouter.java</files>
  <read_first>
    - src/main/java/com/ae2channelrouter/AE2ChannelRouter.java
  </read_first>
  <action>
    Register the custom renderer in the mod initialization.

    Location: AE2ChannelRouter.java

    In the FMLInitializationEvent (init method):
    1. Import ClientRegistry
    2. Register the rendering handler

    Add to initialization:
    ```java
    // Register custom block renderer
    ClientRegistry.bindTileEntitySpecialRenderer(RoutingCableTile.class, new RoutingCableRender());
    ```

    Also register the render ID:
    ```java
    // Get and store render ID for the cable
    int renderId = RenderingRegistry.getNextAvailableRenderId();
    // Store for use in block
    ```

    Note: For 1.7.10, use:
    - RenderingRegistry.registerBlockHandler() for custom rendering
  </action>
  <acceptance_criteria>
    - AE2ChannelRouter.java imports ClientRegistry
    - Renderer is registered for RoutingCableTile
    - Render ID is obtained and stored
  </acceptance_criteria>
  <verify>
    <automated>grep -n "RoutingCableRender" src/main/java/com/ae2channelrouter/AE2ChannelRouter.java</automated>
  </verify>
  <done>Custom renderer registered in mod initialization</done>
</task>

<task type="auto">
  <name>Task 3: Update RoutingCableBlock for custom rendering</name>
  <files>src/main/java/com/ae2channelrouter/block/RoutingCableBlock.java</files>
  <read_first>
    - src/main/java/com/ae2channelrouter/block/RoutingCableBlock.java
  </read_first>
  <action>
    Update RoutingCableBlock to use custom rendering.

    Changes needed:
    1. Add field for render ID
    2. Override getRenderType() to return custom render ID
    3. Set block hardness and resistance similar to AE2 cables

    Add:
    ```java
    // In RoutingCableBlock class
    private static int renderId = -1;

    public static int getRenderId() {
        return renderId;
    }

    public static void setRenderId(int id) {
        renderId = id;
    }

    @Override
    public int getRenderType() {
        return renderId;
    }
    ```

    Set block properties:
    - Hardness: 0.3f (similar to AE2 glass cable)
    - Resistance: 1.0f
    - Step sound: soundTypeGlass
  </action>
  <acceptance_criteria>
    - RoutingCableBlock has getRenderType() override
    - Block returns custom render ID
    - Block has appropriate hardness/resistance
  </acceptance_criteria>
  <verify>
    <automated>grep -n "getRenderType" src/main/java/com/ae2channelrouter/block/RoutingCableBlock.java</automated>
  </verify>
  <done>RoutingCableBlock configured for custom rendering</done>
</task>

<task type="auto">
  <name>Task 4: Add client package directory</name>
  <files>src/main/java/com/ae2channelrouter/client/render/.gitkeep</files>
  <read_first>
    - None
  </read_first>
  <action>
    Ensure client package structure exists. The directory structure should be:
    - src/main/java/com/ae2channelrouter/client/
    - src/main/java/com/ae2channelrouter/client/render/

    If not exists, create placeholder or ensure directory exists.
  </action>
  <acceptance_criteria>
    - Client render package exists
    - Directory structure is valid Java package
  </acceptance_criteria>
  <verify>
    <automated>ls -la src/main/java/com/ae2channelrouter/client/</automated>
  </verify>
  <done>Client package structure verified</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `./gradlew build` to verify compilation
2. Check for any rendering-related errors
3. Verify cable renders with orange color in-game
</verification>

<success_criteria>
- RoutingCableRender implements custom rendering
- Orange glass style is visible
- Renderer is registered in mod initialization
- Build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-routing-cable/02-02-SUMMARY.md`
</output>
